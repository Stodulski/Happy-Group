<!DOCTYPE html>
<html lang="en">
    <head>
        <link rel="stylesheet" href="public/css/partials/normalize.css" />
        <link rel="stylesheet" href="public/css/register.css" />
        <link rel="shortcut icon" href="/public/img/favicon.ico" type="image/x-icon">
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>HappyGroup - Registrarse</title>
    </head>
    <body>
        <main class="form">
            <form action="/register" method="post" class="form__container">
                <% if (errorMsg.length > 0 ) { %>
                <h1 class="error"><%= errorMsg %></h1>
                <% } else {%>
                <h1>Registrarse</h1>
                <% }%>
                <input
                    type="text"
                    class="form__username"
                    name="username"
                    placeholder="Usuario"
                    id="username"
                />
                <input
                    type="password"
                    class="form__password"
                    name="password"
                    placeholder="Contraseña"
                    id="password"
                />
                <input
                    type="password"
                    class="form__password-repeat"
                    name="rePassword"
                    placeholder="Repetir contraseña"
                    id="rePassword"
                />
                <input
                    type="submit"
                    value="Enviar"
                    class="form__submit"
                    id="submit"
                    disabled
                />
                <span>¿Tienes cuenta? <a href="/login">Iniciar sesion</a></span>
            </form>
        </main>
        <script src="public/js/register.js"></script>
    </body>
</html>
